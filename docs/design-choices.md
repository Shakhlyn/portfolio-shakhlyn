1. Special format(v1) and premium(light & dark mode)
2. the page can be like `leather parchment`
    - When the home page opens, it opens like a leather journal.
    - When the user goes to the new page, it'll be like next page of the leather journal.


---
---
---

Inspiration:
1. https://red1-for-hek.vercel.app/
2. https://sawad.framer.website/
3. https://portfolio-site-git-main-akash-kumar5s-projects.vercel.app/



---
---

### parchment design and flipping behavior

The leather parchement should be one-page book. the flipping should be from the bottom to the top, and the previous page should get flipped back to the parchement.

Sample: ![alt page-structure](image-2.png)
---

- ***Phase 0***: The first view, the buffering phase, the ![alt parchment](image.png). a knot around ![alt knob](image-1.png) should be unfolding during the beffering time.

- ***Phase 1***: After buffering, the hero section displays. It should be a one-page parchment, not book like two-page. The hero section should contain the `intro` with name and the `profile picuture`. It also should display the role like software engineer with a short, eye-catching intro just below the name and the role title.
    - It should have two buttons for `contact` and `downlod resume` link.

    - You can add some nitty-gritty or small text if you think it would be better.


- ***Phase 2***: If users `scroll down` or click `down button`, 
Should have a page view for the current role. What I do, how I do, what I improve. this is also for one page.


- ***Phase 3***: If users `scroll down` or click `down button` more, the users will go the next poge: Project page. This project page might have more projects to display in single page. this page should have a behavior like 'horizontal scrolling'. But not that simple scrolling; If user scroll down or click the down button the page go right and the page-roll, is in the right side of the page, gets open continuously as user clicks down button or scroll down, and vice-versa for the scroll up or top arrow button.