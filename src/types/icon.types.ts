import type { SVGProps } from 'react';

/**
 * Icons are decorative by default — the control that wraps them carries the
 * accessible name (docs/3-style-preference.md §10).
 */
export type IconProps = SVGProps<SVGSVGElement>;
