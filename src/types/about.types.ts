export interface AboutType {
  /**
   * Prose paragraphs, rendered in order at full container width. Two or three
   * (docs/3-style-preference.md §6.5) — no accordion, no "read more".
   */
  paragraphs: string[];
}
